<!DOCTYPE html>
<html lang="en">

<head>
    <title></title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="css/reset.css" rel="stylesheet">
    <link href="css/login.css" rel="stylesheet">
</head>

<body>
    <div class="login">
        <div class="loginLogo"></div>
        <div class="loginAccount" id="loginAccount">
            <h3>欢迎登陆</h3>
            <h6>请输入您的账号</h6>
            <input id="account" type="text" placeholder="电子邮件、电话、账号" style="outline:none;">
            <ol>
                <li class="register">
                    没有账号 ? <a href="#">立即注册 -></a>
                </li>
                <li class="forget">
                    <a href="#">忘记用户名？</a>
                </li>
            </ol>
            <div class="bottomButton">
                <input id="cancel" class="cancel" type="button" value="取消">
                <input id="next" type="button" value="下一步">
            </div>
        </div>
        <div class="loginPassword hide" id="loginPassword">
            <h3>欢迎登陆</h3>
            <h6>请输入您的密码</h6>
            <input id="passWord" type="password" placeholder="密码" style="outline:none;">
            <ol>
                <a href="#">忘记密码？</a>
            </ol>
            <a id="loginLink"><input type="button" id="login" value="登陆"></a>

        </div>
    </div>
</body>
<script src="./js/jquery-1.12.4.js"></script>
<script>
    var teacherAccount = ["lancer88888", "meecpcgadmin01", "meecpcgadmin02", "meebjjsadmin01", "meebjjsadmin02", "meeszftadmin01", "meeszftadmin02", "meezzbzadmin01",
"meezzbzadmin02","meetskpadmin01","meetskpadmin02","meejsszadmin01","meejsszadmin02","meebj81admin01","meebj81admin02"]

    var isTeacher = false

    $('#cancel').click(function () {
        $('#account').val('')
    })
    $('#next').click(function () {
        $('#loginPassword').removeClass('hide');
        $('#loginAccount').addClass('hide');
    })
    $('#login').click(function () {
        var account = $('#account').val();
        var passWord = $('#passWord').val();
        console.log(account + '-----' + passWord)

        for (var i = 0; i < teacherAccount.length; i ++) {      //遍历账户密码
            if (account === teacherAccount[i]) {   //如果有账号
                if (passWord === "mee123") {
                    // $('#loginLink').attr('href', '/teacher');
                    localStorage.setItem("isLogin",1)
                    isTeacher = true
                    location.href = "/teacher"
                }
                else {
                    alert("密码错误！")
                    return false
                }
            }
        }

        isTeacher || (location.href = "/student")

        // if (account == 'lancer88888') {
        //     $('#loginLink').attr('href', '/teacher');
        //     localStorage.setItem("isLogin",1)
        // } else {
        //     $('#loginLink').attr('href', '/student');
        // }
    })
</script>
</html>